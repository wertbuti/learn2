یک file مجموعه داده های ذخیره شده در یک دیسک با نام و مسیر مشخص می باشد. وقتی که فایل برای خواندن یا نوشتن باز می شود، یک stream می شود.

هم با StreamWriter و StreamReader میتوان فایلها را خواند و نوشت
هم با File میتوان فایلها را خواند و نوشت

مراجعه شود به پروژه ReadingWritingFile_WinFormsApp_Net------------------------------------------