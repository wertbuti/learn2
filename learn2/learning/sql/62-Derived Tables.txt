Derived Tables
جداول مشتق شده به جداولی میگویند که از جدول دیگری مشتق میشوند و یک نام به آن میدهند. این جداول در مقابل کلاز FROM آورده می شوند
Select * from (Select * From t1 where Col2 =2) A
در کد بالا جدول مشتق شده ای به نام A وجود دارد