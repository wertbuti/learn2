انواع جداول در اس کیوال
1) ساخت یافته یا Structured
2) درهم یا Heap یا  Hash یا Stack
داده ها در جداول ساخت یافته با نظم خاصی  ذخیره میشوند اما داده ها در جداول درهم، بدون نظم ذخیره میشوند
برای اینکه یک جدول ساخت یافته باشد باید یک کلید اصلی یا همان PrimaryKey برای آن تعریف کرد که این کلید ایندکس میباشد